// Using Math

const raio = 5.6;
/* PI is a constant within math,
 multiplied by math power, followed by
parameters radius and 2 which means high radius squared.
Math has a constant named PI.
*/
const area = Math.PI * Math.pow(raio,2);

console.log(area);

// The math is an object.
console.log(typeof Math)