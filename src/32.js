// Regular Expressions: Match Anything with Wildcard Period (FreeCodeCamp)

let exampleStr = "Let's have fun with regular expressions!";
let unRegex = / .un./i;  // Change this line

let result = unRegex.test(exampleStr);