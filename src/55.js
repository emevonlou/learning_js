// Ternary operators

/*
const result = note => {
    return note >= 7 ? 'Approved' : 'Reproved';
}

*/ 

const result = note => note >= 7 ? 'Approved' : 'Reproved';

console.log(result(7.1));
console.log(result(6.3));
